# Solar Origin Prototype

太陽を原点とした宇宙シミュレーションの **実装プロトタイプ** です。  
Android / Windows を最終ターゲットにしつつ、まずはローカルで動くシミュレーションコアを Python で実装しています。

## 実装内容（MVP+）
- Sun + 8惑星のN体シミュレーション（Leapfrog積分）
- 重力定数スケール変更 (`--gravity-scale`)
- スナップショット保存/復元
- 1日刻みの時間進行デモCLI
- 物理品質モニタリング（エネルギードリフト、運動量ノルム、重心表示）
- 入力検証（不正ステップ幅、不正重力スケール、重複天体名など）

## 構成
- `src/solar_sim/bodies.py`: 天体データ構造
- `src/solar_sim/engine.py`: N体計算エンジン（検証・保存復元・保存量計測）
- `src/solar_sim/scenario.py`: 太陽系初期シナリオ
- `src/solar_sim/cli.py`: 実行CLI（品質指標出力）
- `tests/test_engine.py`: 基本動作 + 品質回帰テスト

## 使い方
```bash
PYTHONPATH=src python -m solar_sim.cli --steps 365 --dt 86400 --gravity-scale 1.0
```

## テスト
```bash
PYTHONPATH=src pytest -q
```

## 取扱説明書
- 日本語マニュアル: `docs/USER_MANUAL_ja.md`

## セルフコードレビュー（第2版）
### 改善した点
- **安全性向上**: シミュレーション設定値のバリデーションを追加し、破綻入力を早期検出。
- **可観測性向上**: CLIにエネルギードリフト率・運動量ノルム・重心を表示して、物理的健全性を確認可能に。
- **回帰防止強化**: エネルギー安定性、運動量保存、例外系をテスト化して品質を底上げ。

### 残課題（次にやるべきこと）
- 現在は惑星初期条件が近似値で、厳密な暦元期（J2000等）を使っていない。
- O(N^2)の全対全計算なので、恒星数を増やすならBarnes-Hut/FMM等が必要。
- Android実運用を見据えると、将来はUnity Jobs/BurstやGPU計算への置換が必要。
- 長期シミュレーションでの誤差制御（可変タイムステップやシンプレクティック高次化）を検討すべき。
